export enum CredentialDocumentTypeEnum {
  EducationDocument = 1,
  IdDocument,
  EmploymentDocument,
  // EducationDocument = 'Degree certificate, diploma or other education document',
  // IdDocument = 'ID document (passport, driving license) and a photo of the applicant holding it',
  // EmploymentDocument = 'Employment verification letter, contract or other document verifying employment',
}
